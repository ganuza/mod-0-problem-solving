// goal:  to create a function that removes EVERY instance of "s" in a string that is given as an argument

// create a function called removeLetterS
// figure out a method to take out s (replaceAll("s", ""))
// print result

function removeLetterS(word) {
    console.log(word.replaceAll("s", ""));
}

removeLetterS("stinks")
