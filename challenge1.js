var names = ["Tim", "Beth", "Harry", "Luke", "Ian"];

for (var i = 0; i < names.length; i++) {
    //console.log(names[i].length);
    if (names[i].length === 4) {
        console.log(names[i]);
    } 
}
